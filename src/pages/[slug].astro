---
import directoryData from '../../seo_directory_data.json';
export async function getStaticPaths() {
  return directoryData.map((page) => ({ params: { slug: page.url_slug }, props: { page } }));
}
const { page } = Astro.props;
const schemaData = { /* Schema definition */ };
---

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://googletagmanager.com"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-1V2WX4K30H');
    </script>
    <meta charset="UTF-8">
    <title>{page.meta_title}</title>
    <!-- Styles and Content omitted for brevity -->
</head>
<body>
    <!-- Main content and Listings mapping -->
</body>
</html>
